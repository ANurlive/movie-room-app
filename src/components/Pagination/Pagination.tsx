import { Component } from 'react';

// pagination is not required for current task, but created in advance

export default class Pagination extends Component {
  render() {
    return <div>Pagination</div>;
  }
}
